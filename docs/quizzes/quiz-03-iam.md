# Quiz 3: IAM and Blast Radius

1. Why is `principalSet://.../POOL/*` risky?
2. Why split staging and production deployer service accounts?
3. What can go wrong if the deployer service account has `roles/editor`?
4. What is the difference between the Cloud Run runtime service account and the GitHub deployer service account?
5. What is the blast radius of a compromised deploy workflow?
