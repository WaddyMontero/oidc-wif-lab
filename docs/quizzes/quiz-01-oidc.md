# Quiz 1: OIDC Basics

1. What does `id-token: write` allow a workflow to do?
2. Why is `aud` important?
3. What is the difference between `iss` and `sub`?
4. Why should the debug workflow print claims but not the raw token?
